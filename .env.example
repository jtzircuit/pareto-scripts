# Copy this file to .env and fill in the values before running any scripts

# Ethereum RPC URL (must support archive mode for historical queries)
# e.g. https://mainnet.infura.io/v3/<YOUR_INFURA_KEY>
# fallback public endpoint: https://eth.drpc.org
RPC_URL=
# Optional comma-separated pool (used for load splitting/failover)
# RPC_URLS=https://ethereum-rpc.publicnode.com/,https://eth.drpc.org,https://eth1.lava.build

# Optional: Etherscan API key for ABI retrieval and block lookup
# You can get one from https://etherscan.io/apis
ETHERSCAN_API_KEY=

# Contract address of the vault/tranche you want to monitor.
VAULT_ADDRESS=0xEC6a70F62a83418c7fb238182eD2865F80491a8B
# Optional alias used by backfill script (CLI --contract-address overrides both)
# CONTRACT_ADDRESS=

# Starting date (ISO format) or starting block number for backfill
START_DATE=2024-01-01
# or START_BLOCK=XXXXX
